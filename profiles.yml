# Checked in deliberately: it contains no secrets. Authentication comes from
# Application Default Credentials (`gcloud auth application-default login`),
# and the project id comes from the environment, so a fork of this repo points
# at the forker's own BigQuery project without editing a file.
ga4_analytics:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('DBT_BIGQUERY_PROJECT') }}"
      dataset: "{{ env_var('DBT_BIGQUERY_DATASET', 'ga4_analytics') }}"
      location: US
      threads: 8
      timeout_seconds: 600
      priority: interactive
      # Per-QUERY ceiling, not a per-build one. The largest single query in
      # this project scans 2.9 GiB (staging over the full source); a whole
      # build bills 6.75 GiB across 106 jobs. A single query reaching 20 GiB
      # means a predicate was lost, so fail rather than quietly bill it.
      maximum_bytes_billed: 21474836480
